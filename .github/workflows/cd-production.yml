name: CD — Production

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  deploy-production:
    name: Deploy to Production
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4
      - name: Build and push images
        run: echo "Build and push to registry — implement in Phase 6"
      - name: Deploy to production server
        run: echo "SSH deploy — implement in Phase 6"
